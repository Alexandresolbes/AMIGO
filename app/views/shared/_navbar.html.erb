<nav class="navbar navbar-expand bg-light">
  <div class="container-fluid justify-content-center">
      <ul class="navbar-nav w-100 justify-content-between py-0">
        <li class="nav-item d-inline-block">
          <% if @trip %>
            <%= link_to trip_path(@trip), class: "nav-link" do %>
              <i class="fa-solid fa-house"></i>
            <% end %>
          <% else %>
            <%= link_to root_path, class: "nav-link" do %>
              <i class="fa-solid fa-house"></i>
            <% end %>
          <% end %>
        </li>
        <% if user_signed_in? %>
          <% if !@trip.nil? && !@trip.id.nil? %>
            <li class="nav-item d-inline-block">
              <%= link_to trip_rooms_path(@trip), class:"nav-link" do  %>
                <i class="fa-solid fa-message"></i>
              <% end %>
            </li>
            <li class="nav-item d-inline-block">
              <%= link_to trip_notifications_path(@trip), class:"nav-link" do  %>
                <i class="fa-solid fa-bell"></i>
              <% end %>
            </li>
          <% end %>

         <li class="nav-item  d-inline-block dropdown">
          <a class="nav-link" href="#" data-bs-toggle="dropdown"><i class="fa-solid fa-user-group"></i></a>
          <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <% if !@trip.nil? && !@trip.id.nil? %>
              <%= link_to "Invite a friend", trip_invite_path(@trip), class: "dropdown-item" %>
               <%= link_to "Random Wheel", trip_random_wheel_path(@trip), class: "dropdown-item", data: {turbo: false} %>
            <% end %>
            <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item d-inline-block ">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>
