<div class="bg-gradient-blue text-center text-light">
<div class="container">
<h1>My Trips</h1>
<% @trips.each do |user_trip|%>
  <h2><%= link_to user_trip.trip.destination, trip_path(user_trip.trip)%></h2>
<% end %>
<%= link_to "Create a trip", new_trip_path %>
</div>
</div>
